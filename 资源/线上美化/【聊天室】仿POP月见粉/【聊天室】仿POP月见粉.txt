/* 顶部栏背景与底边 */
.page-header {
    background: #F0F0F0 !important;
    border-bottom: 1px solid #C0C0C0 !important;
}

/* 返回按钮 + "消息"文字 */
.page-header-content > .page-back-btn:first-child {
    color: #D4B5B5;
    display: inline-flex;
    align-items: center;
    white-space: nowrap;
    position: relative;
    left: -12px;
}
.page-header-content > .page-back-btn:first-child svg {
    flex-shrink: 0;
    width: 32px;
    height: 32px;
}
.page-header-content > .page-back-btn:first-child::after {
    content: "消息";
    margin-left: -3px;
    font-size: 16px;
    color: #D4B5B5;
    position: relative;
    top: 1px;
}

/* 标题与副标题 */
.page-title {
    font-size: 17px;
}
.page-title::after {
    content: "刚刚";
    display: block;
    font-size: 12px;
    color: #999;
    text-align: center;
    line-height: 1.2;
}

/* 右侧更多按钮（替换为头像） */
.page-header-right .page-back-btn {
    width: 34px;
    height: 34px;
    border-radius: 50%;
    background-image: url('https://i.ibb.co/hJ6gkpMD/IMG-20260408-220056.jpg');
    background-size: cover;
    background-position: center;
}
.page-header-right .page-back-btn svg {
    display: none;
}
.page-header-right {
    position: relative;
    right: -6px;
}

/* 气泡基础 */
.chat-bubble-role-user,
.chat-bubble-role-assistant {
    border-radius: 16px !important;
    position: relative !important;
    min-width: 40px !important;
    max-width: 280px !important;
}

/* 用户气泡（普通文本） */
.chat-bubble-role-user:not(.chat-bubble-media):not(.voice-msg-bubble) {
    background: #F3E2E6 !important;
    color: #000000 !important;
    padding: 5px 28px 5px 12px !important;
    margin: 0 !important;
    border: 1px solid #F3E2E6 !important;
    border-radius: 16px 16px 5px 16px !important;
}

/* 用户连续消息 */
.chat-msg-wrapper[data-role="user"][data-consecutive] .chat-bubble-role-user:not(.chat-bubble-media):not(.voice-msg-bubble) {
    border-radius: 16px 5px 5px 16px !important;
}

/* 用户气泡尾部装饰 */
.chat-bubble-role-user:not(.chat-bubble-media):not(.voice-msg-bubble)::after {
    content: '';
    position: absolute;
    right: 0;
    bottom: 0;
    width: 22px;
    height: 22px;
    background-image: url('https://i.ibb.co/fzMP2XGb/IMG-20260626-021239.png');
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
    transform: translate(-1px, -3px);
    pointer-events: none;
    z-index: 10;
}

/* 对方气泡（普通文本） */
.chat-bubble-role-assistant:not(.chat-bubble-media):not(.voice-msg-bubble) {
    background: #FFFFFF !important;
    color: #000000 !important;
    padding: 5px 12px !important;
    margin: 0 !important;
    border: 1px solid #DCDCDC !important;
    border-radius: 16px 16px 16px 5px !important;
}

/* 对方连续消息 */
.chat-msg-wrapper[data-role="assistant"][data-consecutive] .chat-bubble-role-assistant:not(.chat-bubble-media):not(.voice-msg-bubble) {
    border-radius: 5px 16px 16px 5px !important;
}

/* 用户头像隐藏 */
.chat-msg-wrapper[data-role="user"] .chat-msg-avatar {
    display: none !important;
}

/* 助手头像占位隐藏 */
.chat-msg-wrapper[data-role="assistant"] .chat-msg-avatar {
    width: 20px !important;
    height: 20px !important;
    min-width: 20px !important;
    flex-shrink: 0 !important;
    border-radius: 50% !important;
    overflow: hidden !important;
    order: 3 !important;
    opacity: 0 !important;            /* ← 改这里 */
    pointer-events: auto !important;  /* ← 加这行 */
}

.chat-msg-wrapper[data-role="assistant"] .chat-msg-avatar img {
    width: 20px !important;
    height: 20px !important;
    object-fit: cover !important;
}

/* 助手消息布局 */
.chat-msg-wrapper[data-role="assistant"] {
    align-items: center !important;
}

.chat-msg-wrapper[data-role="assistant"] .chat-msg-content-wrap {
    order: 1 !important;
}

/* 内心独白按钮 */
.chat-msg-wrapper[data-role="assistant"] .chat-monologue-heart {
    order: 2 !important;
    align-self: center !important;
    margin-left: 4px !important;
    margin-right: 4px !important;
    flex-shrink: 0;
}

/* 内心独白卡片偏移 */
.chat-thought-card {
    margin-left: -1px !important;
}

/* 连续消息间距 */
.chat-msg-wrapper[data-consecutive] {
    margin-top: -14px !important;
}

/* 时间戳/系统消息 */
.chat-sys-msg {
    color: rgba(255, 255, 255, 0.85) !important;
}

/* 引用消息容器 */
.chat-quote-preview {
    background: transparent !important;
    border-left: none !important;
    position: relative;
    padding-left: 14px !important;
    padding-top: 20px !important;
    padding-bottom: 2px !important;
    font-size: 12.8px !important;
    color: #252525 !important;
    overflow: visible !important;
}

/* 左侧竖条 */
.chat-quote-preview::before {
    content: '';
    position: absolute;
    left: 4px;
    top: 2px;
    bottom: 2px;
    width: 1.5px;
    border-radius: 1px;
}

/* 文字标签 */
.chat-quote-preview::after {
    position: absolute;
    left: 14px;
    top: 0;
    font-size: 12.8px;
    font-weight: bold;
    white-space: nowrap;
}

/* 对方引用颜色 */
.chat-msg-wrapper[data-role="assistant"] .chat-quote-preview::before {
    background: #D4B5B5;
}
.chat-msg-wrapper[data-role="assistant"] .chat-quote-preview::after {
    color: #D4B5B5;
    content: "我";
}

/* 用户引用颜色 */
.chat-msg-wrapper[data-role="user"] .chat-quote-preview::before {
    background: #D4B5B5;
}
.chat-msg-wrapper[data-role="user"] .chat-quote-preview::after {
    color: #D4B5B5;
    content: "对方";
}

/* 底部栏背景与上边框 */
.chat-input-bar {
    background: #F0F0F0 !important;
    border-top: 1px solid #C0C0C0 !important;
}

/* 输入框样式 */
.chat-input-textarea {
    background: #F9F9F9 !important;
    border: 1.2px solid #DCDCDC !important;
    border-radius: 24px !important;
    padding: 8px 12px !important;
    resize: none !important;
    overflow-y: hidden !important;
    overflow-x: hidden !important;
}

/* 底部栏所有按钮图标颜色 */
.chat-input-actions .ui-bare-btn {
    color: #C0C0C0 !important;
}

/* 发送按钮禁用状态稍浅 */
.chat-input-actions .ui-bare-btn:disabled {
    color: #C0C0C0 !important;
}
